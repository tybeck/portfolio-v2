div(#container="", [class]="classesAvailable")
  span([innerText]="type")
  div(class="progress-container")
    div(
      class="progress-bar",
      [ngStyle]="{ 'background-color': color, width: lvl }"
    )
      span([innerText]="lvl")
